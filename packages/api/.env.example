# Gaz Time API Environment Variables

# Server Configuration
PORT=3333
HOST=0.0.0.0
LOG_LEVEL=info

# CORS Configuration
# By default, only Gaztime domains are allowed (gaztime.app, admin.gaztime.app, etc.)
# Set CORS_ORIGIN to add an additional custom origin (useful for testing)
# CORS_ORIGIN=https://your-custom-domain.com

# JWT Secret (REQUIRED in production - change this!)
JWT_SECRET=your-super-secret-key-change-in-production

# Database
DATABASE_URL=./data/gaztime.db

# Node Environment
NODE_ENV=development

# Future: Real-time & Background Jobs
# REDIS_URL=redis://localhost:6379
# SOCKET_IO_PORT=3334
